let x  = input([1, 3]);
let W1 = input([3, 4]);
let b1 = input([1, 4]);
let W2 = input([4, 2]);
let b2 = input([1, 2]);

let z1     = x @ W1 + b1;
let h      = tanh(z1);
let logits = h @ W2 + b2;

output logits;
